pip install Flask-PyMongo
pip install Flask
pip install dnspython # per risolvere l'indirizzo del db
npm install -g --force nodemon
pip install flask-cors
pip install shapely
npm i -g nodemon  //nodemon main.py